<UserControl x:Class="MessagingModule.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:regions="http://www.codeplex.com/CompositeWPF"
			 Background="AliceBlue" mc:Ignorable="d"  d:DesignWidth="500" d:DesignHeight="248" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="100"/>
            <RowDefinition Height="100" MinHeight="100"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <ContentControl regions:RegionManager.RegionName="ConversationHistoryRegion" d:IsLocked="True"/>
        <ContentControl regions:RegionManager.RegionName="ClientListRegion" Grid.Column="1" d:IsLocked="True"/>
        <ContentControl regions:RegionManager.RegionName="SendMessageRegion" Grid.Row="1" Grid.ColumnSpan="2" d:IsLocked="True"/>
    </Grid>
</UserControl>
