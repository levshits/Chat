<UserControl x:Class="MessagingModule.Views.ChatClientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150"
             >
	<UserControl.Resources>
		<DataTemplate x:Key="UserItemTemplate">
			<StackPanel Orientation="Horizontal">
				<Image/>
				<TextBlock FontSize="16" FontFamily="Segoe UI Emoji"/>
				<TextBlock FontSize="16"/>
			</StackPanel>
		</DataTemplate>
	</UserControl.Resources>
    <Grid>
    	<ListBox Margin="3,30,3,3" Background="#84FFFFFF" ItemTemplate="{DynamicResource UserItemTemplate}"/>
    	<TextBlock Margin="10,3,10,0" TextWrapping="Wrap" Text="Users" VerticalAlignment="Top" FontSize="16" TextAlignment="Center" FontFamily="Segoe UI Light"/>
            
    </Grid>
</UserControl>
